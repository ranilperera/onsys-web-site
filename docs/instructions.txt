Content lives in two places, and the distinction matters:

Where	What it is
Source of truth	seed-content.ts — 4,784 lines	In git, reviewable, survives a database rebuild
What the site renders	Postgres pages table	Populated by npm run seed from the file above
The page you have open, [slug]/page.tsx, holds no copy at all — it calls getPage(slug) and renders whatever the database returns. That one file serves all 29 content pages.

Which pages are database-driven
Editable in seed-content.ts (page entries start at line 3948): home, expertise, about, contact, products, pricing-and-plans, managed-database-services, on-call-dba-services, emergency-database-support, database-consultancy, database-upgrades-migrations-dr, remote-database-support, free-20-point-sql-server-health-check, all the managed-IT / cloud / software / security service pages, plus privacy, terms and disclaimer.

Hardcoded React — edit the .tsx directly: /book, /book/cancel, /client-portal, the /blog listing, and /admin/*.

Blog posts live in the database and are editable through the admin console at /admin/posts.

Yes, you can edit it manually — here's the shape
Each page is a plain object. For example, emergency-database-support:


slug: 'emergency-database-support',
heading: 'Emergency database support',
eyebrow: 'Outage response · answered 24/7',
lede: `Production down, data at risk...`,
seoTitle: 'Emergency DBA Support Australia | 24/7 Response',
seoDescription: 'Production database down? Call 1800 431 416 for...',
blocks: emergencyBlocks,
faqs: [ { question: '...', answer: '...' }, ... ],
Two levels of edit:

Hero, SEO fields and FAQs — plain strings right there in the page entry. Change the text and you're done.
Body copy — that's the blocks: reference. The block arrays sit higher in the same file: emergencyBlocks at line 1266, homeBlocks at line 20, pricingBlocks at line 728, and so on. They're typed against the shared zod schema, so npm run typecheck -w @onsys/api will catch a malformed block before it reaches the database.
Note ${org.phone} in that lede — phone, email and booking URL come from config, so never hardcode 1800 431 416 into copy; use the interpolation.

Applying a change

# locally: edit seed-content.ts, then
npm run typecheck -w @onsys/api

# on the VM, after git pull && up -d --build api
docker compose -f docker-compose.prod.yml exec api npm run seed -w @onsys/api
docker compose -f docker-compose.prod.yml exec api npm run embeddings:build -w @onsys/api -- --all
docker compose -f docker-compose.prod.yml up -d --force-recreate web
The seed upserts by slug, so it's safe to re-run. Pages carry revalidate = 300, so edits surface on their own within five minutes — the --force-recreate just makes it immediate. The embeddings rebuild is the --all case from the runbook: you edited existing content, so incremental would silently skip it.

Two things not to do
Don't edit the database directly with SQL. It works until the next npm run seed, which upserts by slug and overwrites your change — and it's not in git, so it vanishes on any rebuild.

Don't reach for the admin console for pages. PUT /api/admin/pages/:id exists in admin.routes.ts, but there's no page editor in the UI — admin only has the dashboard, posts, leads, chat and login. Anything written through that endpoint has the same overwrite problem.

If you'd like a page editor in the admin console so content changes don't need a deploy, that's a real piece of work but the API half already exists — say the word and I'll scope it.


cd /opt/onsys
git pull
docker compose -f docker-compose.prod.yml up -d --build api
docker compose -f docker-compose.prod.yml exec api npm run seed -w @onsys/api
docker compose -f docker-compose.prod.yml exec api npm run embeddings:build -w @onsys/api -- --all
docker compose -f docker-compose.prod.yml up -d --force-recreate web

=============================================


1. Add the new environment variables

ssh onsyswebuer@vm-aueast-onsys-web-04
cd /opt/onsys
nano .env
Add these — all optional, each degrades independently:


# Health check booking lead time (days). Default 7.
HEALTHCHECK_LEAD_DAYS=7

# Client portal — leave unset until DBPulse is live
ORG_PORTAL_ENABLED=false

# Stripe — leave ALL THREE unset for now. The emergency page no longer
# takes payment; the endpoint stays wired for prepaid hour bundles.
# STRIPE_SECRET_KEY=
# STRIPE_EMERGENCY_PRICE_ID=
# STRIPE_WEBHOOK_SECRET=
Also confirm this is present, since the disclaimer and privacy pages cite it:


grep ORG_LEGAL_NAME .env    # should be: ORG_LEGAL_NAME=Onsys Pty Ltd
2. Pull, build, migrate

cd /opt/onsys
git pull
docker compose -f docker-compose.prod.yml up -d --build
docker compose -f docker-compose.prod.yml logs -f api | grep -i migrat
Three migrations apply on API startup: add_emergency_requests, add_emergency_lead_source, add_author. Wait for them before continuing.

3. Content and data
Order matters — seed before embeddings:


C="docker compose -f docker-compose.prod.yml exec api"

# New pages, rewritten health check, retargeted titles, legal name fix
$C npm run seed -w @onsys/api

# Author identity — dry run first
$C npm run seed:author -w @onsys/api -- --dry-run
$C npm run seed:author -w @onsys/api -- --role="Your Title"

# ~49 dead blog URLs from the WordPress migration
$C npm run redirects:blog -w @onsys/api -- --dry-run
$C npm run redirects:blog -w @onsys/api

# Title suffixes + sitemap lastmod; reports placeholder text, changes none
$C npm run fix:imported -w @onsys/api -- --dry-run
$C npm run fix:imported -w @onsys/api

# Content changed, so this is the --all case
$C npm run embeddings:build -w @onsys/api -- --all

# Clear the on-disk fetch cache
docker compose -f docker-compose.prod.yml up -d --force-recreate web
4. Point HAProxy at the real health check

=======================

To create or reset the account
create:admin upserts by email — same command either way. PowerShell eats the flags, so you need --%:


npm run create:admin --% -- --email=ranil@onsys.com.au --name="Ranil Perera" --role=ADMIN
Or, if that misbehaves, call the script directly:


cd apps\api
npx tsx src/scripts/create-admin.ts --email=ranil@onsys.com.au --name="Ranil Perera" --role=ADMIN
It prompts for the password — minimum 12 characters. Leave --password off so it stays out of your shell history.

To create a second account instead, just use a different email. --role=EDITOR gives content access without user management.

Signing in is now two steps
Your .env has all three GRAPH_* values set, so the second-factor email will actually send to ranil@onsys.com.au:

http://localhost:3000/admin/login — email + password
A 6-digit code arrives by email; enter it
If the email doesn't turn up, the code is also written to the API console — I made that dev-only on purpose, so it never happens in production:


[warn] Email not configured — admin code logged for local development only
Both your Postgres (5433, healthy) and API (200 on /health) are already up, so nothing else to start.

Optional: skip the email round-trip
Once you're in, My account → Set up authenticator app gives you a QR code and 10 recovery codes — faster than waiting for an email every login.

Or enrol from the CLI without logging in first:


npm run setup:mfa --% -- --email=ranil@onsys.com.au
That draws the QR in your terminal. It's the same break-glass path you'll want on the VM, so it's worth testing here first.

Then /admin/posts/new is the new editor, ready for a ChatGPT paste.
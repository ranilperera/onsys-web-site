# Nginx reverse proxy for the Onsys platform on an Azure Linux VM.
#
#   sudo cp infra/nginx/onsys.conf /etc/nginx/sites-available/onsys
#   sudo ln -s /etc/nginx/sites-available/onsys /etc/nginx/sites-enabled/
#   sudo certbot --nginx -d onsys.com.au -d www.onsys.com.au
#   sudo nginx -t && sudo systemctl reload nginx

upstream onsys_web { server 127.0.0.1:3000; keepalive 32; }
upstream onsys_api { server 127.0.0.1:4000; keepalive 32; }

# Rate limit zones — a second line of defence behind the app's own limiter.
limit_req_zone $binary_remote_addr zone=onsys_general:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=onsys_api:10m rate=10r/s;

server {
    listen 80;
    listen [::]:80;
    server_name onsys.com.au www.onsys.com.au;
    # Certbot writes its challenge here; everything else goes to HTTPS.
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 301 https://www.onsys.com.au$request_uri; }
}

# Canonical host: force apex → www so Google sees one origin.
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name onsys.com.au;

    ssl_certificate     /etc/letsencrypt/live/onsys.com.au/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/onsys.com.au/privkey.pem;

    return 301 https://www.onsys.com.au$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name www.onsys.com.au;

    ssl_certificate     /etc/letsencrypt/live/onsys.com.au/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/onsys.com.au/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_stapling on;
    ssl_stapling_verify on;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    client_max_body_size 12M;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml application/json application/javascript
               application/xml+rss text/javascript image/svg+xml;

    # Hashed build assets are immutable — cache them hard.
    location /_next/static/ {
        proxy_pass http://onsys_web;
        proxy_cache_valid 200 365d;
        add_header Cache-Control "public, max-age=31536000, immutable";
        access_log off;
    }

    location /api/ {
        limit_req zone=onsys_api burst=20 nodelay;
        proxy_pass http://onsys_api;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection        "";
        proxy_read_timeout 60s;
    }

    location /health {
        proxy_pass http://onsys_api/health;
        access_log off;
    }

    location / {
        limit_req zone=onsys_general burst=50 nodelay;
        proxy_pass http://onsys_web;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
    }
}
